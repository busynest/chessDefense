import { customElement, html, LitElement, TemplateResult } from "lit-element";

@customElement('square-space')
export class SquareSpace extends LitElement {

  constructor() {
    super();
  }

  protected render():TemplateResult {
    return html`


      `
  }
}
